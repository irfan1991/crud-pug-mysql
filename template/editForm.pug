<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Demo CRUD MariaDB
    body
        h2 Form Edit Data

        form(action="/edit", method="post")
            div
                label(for="buku_id") Kode
            div
                input(type="text", name="buku_id", size="10", value=book['buku_id'], readonly="true")
            div
                label(for="buku_judul") Judul
            div
                input(type="text", name="buku_judul", size="30" , value=book['buku_judul'])
            div
                label(for="buku_penulis") Penulis
            div
                input(type="text", name="buku_penulis", size="20", value=book['buku_penulis'])
            div
                label(for="buku_penerbit") Penerbit
            div
                input(type="text", name="buku_penerbit", size="25", value=book['buku_penerbit'])
            
            div(style="margin-top:10px;")
                input(type="submit", value="Simpan")
                input(type="button", value="Batal", onClick="javascript:history.go(-1);")
                
            script console.log(book)